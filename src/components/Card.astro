---
export interface Props {
  imagePath: string;
  quote: string;
  genre: "motivation" | "prayer";
  date: `${number}${number}/${number}${number}/${number}${number}${number}${number}`;
  contributor: string;
  speaker: `${string} ${string}`;
}

const { imagePath, quote, genre, date, contributor, speaker } =
  Astro.props as Props;

const img = await import(imagePath);
---

<!-- start of [quote name] -->
<article class="col-12 col-md-6 tm-post">
  <hr class="tm-hr-primary" />
  <a href="#" class="effect-lily tm-post-link tm-pt-60">
    <div class="tm-post-link-inner">
      <!-- Link the respective image here -->
      <img src={img.default} alt="Image" class="img-fluid" />
    </div>
    <span class="position-absolute tm-new-badge">New</span>
    <h2 class="tm-pt-30 tm-color-primary tm-post-title">
      {quote}
    </h2>
  </a>
  <div class="d-flex justify-content-between tm-pt-45">
    <!-- give genre as motivation for motivation and prayer for prayer -->
    <span class="tm-color-primary">{genre}</span>
    <!-- date format dd/mm/yyyy -->
    <span class="tm-color-primary">{date}</span>
  </div>
  <hr />
  <div class="d-flex justify-content-between">
    <span>by {contributor}</span>

    <!-- if no author please remove this section -->
    <span>said by {speaker}</span>
  </div>
</article>

<!-- End of [quote name] -->
